# Publicly exposed base URL for the application
NEXT_PUBLIC_API_URL=http://localhost:3000

# Connection string for MongoDB
MONGODB_URI=mongodb://localhost:27017/exployee-management

# Secret used for signing JWTs (must be a strong, unique value in every environment)
JWT_SECRET=please-set-a-strong-random-secret
